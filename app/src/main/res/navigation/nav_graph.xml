<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    android:label="@string/app_name"
    app:startDestination="@id/directControl">

    <fragment
        android:id="@+id/directControl"
        android:name="de.drachenfels.gcontrl.ControlFragment"
        android:label="@string/app_name"
        tools:layout="@layout/fragment_direct_control" />
    <fragment
        android:id="@+id/settingsFragment"
        android:name="de.drachenfels.gcontrl.PreferencesFragment"
        android:label="@string/preferences" />
</navigation>